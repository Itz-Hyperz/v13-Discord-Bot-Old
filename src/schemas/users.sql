CREATE TABLE users (
    userid varchar(255),
    warns INT,
    kicks INT,
    bans INT,
    mutes INT,
    joins INT,
    leaves INT,
    messages INT,
    delMessages INT,
    isMuted varchar(255),
    muteTime INT,
    isAfk varchar(255),
    balance INT,
    bank INT,
    workCooldown varchar(255),
    crimeCooldown varchar(255),
    robCooldown varchar(255)
);